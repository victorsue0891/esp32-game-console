# ESP32 復古掌機 — 使用者手冊

## 目錄

1. [首次使用](#1-首次使用)
2. [按鈕說明](#2-按鈕說明)
3. [準備 SD 卡](#3-準備-sd-卡)
4. [開機流程](#4-開機流程)
5. [遊戲選單](#5-遊戲選單)
6. [遊戲中操作](#6-遊戲中操作)
7. [暫停選單](#7-暫停選單)
8. [存檔與讀檔](#8-存檔與讀檔)
9. [音量控制](#9-音量控制)
10. [設定畫面](#10-設定畫面)
11. [主題切換](#11-主題切換)
12. [WiFi 與 OTA 韌體更新](#12-wifi-與-ota-韌體更新)
13. [電池資訊](#13-電池資訊)
14. [支援的遊戲格式](#14-支援的遊戲格式)
15. [常見問題排除](#15-常見問題排除)

---

## 1. 首次使用

你需要準備以下物品：

- ESP32-S3-DevKitC-1-N16R8 主機板（已燒錄韌體）
- Micro SD 卡（FAT32 格式，建議 Class 10）
- NES 或 GameBoy 的 ROM 遊戲檔案
- 3.7V 鋰電池（選用，也可用 USB 供電）

快速上手步驟：

1. 將 SD 卡格式化為 **FAT32**
2. 在 SD 卡中建立資料夾結構（見[第 3 節](#3-準備-sd-卡)）
3. 將 ROM 遊戲檔案複製到對應的資料夾
4. 插入 SD 卡、接上電源
5. 在開機畫面按 **START** 進入遊戲選單
6. 選擇你要玩的遊戲，按 **A** 啟動

---

## 2. 按鈕說明

主機共有 **10 個按鈕**：

```
        [VOL+]  [VOL-]          ← 音量鍵（上方）

         [UP]
  [LEFT]      [RIGHT]           ← 方向鍵（十字鍵）
        [DOWN]

  [SELECT]  [START]             ← 功能鍵（中央）

       [B]  [A]                 ← 動作鍵（右側）
```

| 按鈕 | 在選單中 | 在遊戲中 |
|------|---------|---------|
| **UP / DOWN** | 上下移動選項 | 遊戲方向操作 |
| **LEFT / RIGHT** | — | 遊戲方向操作 |
| **A** | 確認 / 啟動遊戲 | 遊戲 A 鍵 |
| **B** | 返回上一頁 | 遊戲 B 鍵 |
| **START** | 開機確認 | 遊戲 Start 鍵 |
| **SELECT** | — | 遊戲 Select 鍵 |
| **SELECT + START** | — | **開啟暫停選單** |
| **VOL+** | 音量增加 5% | 音量增加 5% |
| **VOL-** | 音量減少 5% | 音量減少 5% |

> 音量鍵在任何畫面都能使用，不需要暫停遊戲。

---

## 3. 準備 SD 卡

### 格式化

- 將 SD 卡格式化為 **FAT32**
- 建議容量：4GB ~ 32GB

### 建立資料夾

請在 SD 卡根目錄建立以下結構：

```
（SD 卡根目錄）
├── roms/
│   ├── nes/            ← 放 NES 遊戲（.nes 檔案）
│   └── gb/             ← 放 GameBoy 遊戲（.gb 檔案）
├── saves/              ← 存檔資料夾（系統會自動建立）
└── system/             ← 系統設定（選用）
    ├── boot.jpg        ← 自訂開機畫面（選用）
    └── wifi.cfg        ← WiFi 設定檔（選用，見第 12 節）
```

### 放入遊戲

- NES 遊戲（副檔名 `.nes`）→ 放到 `roms/nes/` 資料夾
- GameBoy 遊戲（副檔名 `.gb`）→ 放到 `roms/gb/` 資料夾

### 預覽圖片（選用）

你可以為每個遊戲放一張同名的 `.jpg` 預覽圖片：

```
roms/nes/Super Mario Bros.nes
roms/nes/Super Mario Bros.jpg     ← 預覽圖，選到此遊戲時會顯示
```

---

## 4. 開機流程

1. **插入 SD 卡** 並接上電源
2. 系統自動初始化硬體（螢幕、音訊、按鈕、SD 卡）
3. 顯示 **開機畫面**
   - 如果 SD 卡中有 `system/boot.jpg`，會顯示該圖片
   - 如果沒有，顯示文字：「ESP32 Game Console — Press START」
4. **按 START** 進入遊戲選單
   - 畫面會有淡出過場效果

---

## 5. 遊戲選單

開機後進入遊戲選單（Game Launcher），畫面分成兩區：

```
┌──────────────────┬───────────┐
│                  │           │
│   ROM 列表       │  預覽圖    │
│   (60% 寬度)     │ (40% 寬度) │
│                  │           │
│  ► Super Mario   │  [圖片]   │
│    Donkey Kong   │           │
│    Zelda         │           │
│    ...           │           │
│                  │           │
│  ⚙ Settings      │           │
└──────────────────┴───────────┘
```

### 操作方式

| 操作 | 說明 |
|------|------|
| **UP / DOWN** | 在遊戲列表中上下移動 |
| **A** | 啟動選中的遊戲 |
| 移到 **⚙ Settings** 按 **A** | 進入設定畫面 |

當你移動到不同的遊戲時，右側會自動顯示該遊戲的預覽圖片（如果有的話）。

### 沒看到遊戲？

- 確認 SD 卡已正確插入
- 確認 ROM 檔案放在 `roms/nes/` 或 `roms/gb/` 中
- 確認副檔名是 `.nes` 或 `.gb`（區分大小寫）

---

## 6. 遊戲中操作

選好遊戲按 A 後，系統會：

1. 從 SD 卡載入 ROM 到記憶體（約 1~3 秒）
2. 自動偵測遊戲類型（NES 或 GameBoy）
3. 啟動對應的模擬器
4. 遊戲畫面會**全螢幕**顯示（自動縮放）

### 遊戲中的按鈕對應

**NES 遊戲：**

| 主機按鈕 | NES 按鈕 |
|---------|----------|
| 方向鍵 | 方向鍵 |
| A | A |
| B | B |
| START | Start |
| SELECT | Select |

**GameBoy 遊戲：**

| 主機按鈕 | GB 按鈕 |
|---------|--------|
| 方向鍵 | 方向鍵 |
| A | A |
| B | B |
| START | Start |
| SELECT | Select |

### FPS 顯示

若在設定中開啟了 **FPS Overlay**，遊戲畫面右上角會顯示即時幀率數字（正常為 60 fps）。

---

## 7. 暫停選單

### 如何呼叫

在遊戲中**同時按下 SELECT + START**，畫面會出現半透明覆蓋的暫停選單：

```
┌──────────────────────────┐
│                          │
│    ┌──────────────┐      │
│    │  ▶ Resume    │      │
│    │  💾 Save State│      │
│    │  ⬆ Load State│      │
│    │  ✕ Exit Game │      │
│    └──────────────┘      │
│                          │
└──────────────────────────┘
```

### 選項說明

| 選項 | 功能 |
|------|------|
| **▶ Resume** | 關閉暫停選單，繼續玩遊戲 |
| **💾 Save State** | 將目前遊戲進度存檔到 SD 卡 |
| **⬆ Load State** | 讀取之前存的進度 |
| **✕ Exit Game** | 結束遊戲，返回遊戲選單 |

使用 **UP/DOWN** 選擇選項，按 **A** 確認。

---

## 8. 存檔與讀檔

### 存檔

1. 遊戲中按 **SELECT + START** 開啟暫停選單
2. 選擇 **💾 Save State** 按 **A**
3. 按鈕文字會短暫變為：
   - **「Saved!」** → 存檔成功
   - **「Save Failed!」** → 存檔失敗（檢查 SD 卡）

### 讀檔

1. 開啟暫停選單
2. 選擇 **⬆ Load State** 按 **A**
3. 按鈕文字會短暫變為：
   - **「Loaded!」** → 讀檔成功，可繼續按 Resume 遊玩
   - **「No Save!」** → 尚未存過檔

### 存檔須知

- 每個遊戲只能保存 **一個** 存檔
- 存檔位置：SD 卡 `saves/` 資料夾（例如 `saves/Super Mario Bros.sav`）
- 存檔包含 checksum 校驗碼，防止檔案損壞
- 使用原子寫入（先寫暫存檔再改名），即使意外斷電也不會損壞舊存檔
- 新的存檔會 **覆蓋** 舊的存檔

---

## 9. 音量控制

### 基本操作

- 按 **VOL+** 增加音量（每按一次 +5%）
- 按 **VOL-** 減少音量（每按一次 -5%）
- 範圍：0%（靜音）到 100%（最大）

### 音量 OSD 顯示

當你按下音量鍵時，畫面左側會彈出音量指示條：

```
┌────────────────────────────────┐
│ ██                             │
│ ██                             │
│ ██        （遊戲畫面）           │
│ ██                             │
│ ▓▓                             │
│ ▓▓                             │
│ 75%                            │
└────────────────────────────────┘
```

- **綠色長條**：代表目前音量
- **百分比數字**：精確數值
- **自動隱藏**：5 秒鐘沒有操作音量後會自動消失

### 預設值

- 開機預設音量：**30%**

---

## 10. 設定畫面

從遊戲選單底部的 **⚙ Settings** 進入。

### 設定選項一覽

| 項目 | 說明 | 操作 |
|------|------|------|
| **← Back** | 返回遊戲選單 | 按 **A** |
| **Theme** | 目前主題名稱 | 按 **A** 可切換 |
| **FPS Overlay** | ON 或 OFF | 按 **A** 切換 |
| **WiFi OTA** | 啟動 WiFi 韌體更新 | 按 **A** 開始 |
| **Battery** | 電池百分比 + 電壓 | 唯讀，每 2 秒更新 |
| **Free Heap** | 剩餘可用記憶體 (KB) | 唯讀，每 2 秒更新 |
| **Firmware** | 韌體版本編號 | 唯讀 |

### 操作方式

- **UP / DOWN** 切換項目
- **A** 確認/觸發動作
- **← Back** 或 **B** 返回遊戲選單

---

## 11. 主題切換

系統內建三套主題配色，在 **設定 → Theme** 中按 A 輪換：

### Dark（深色主題）

- 深藍灰色背景 + 白色文字 + 綠色強調
- 適合：光線較暗的環境
- 這是預設主題

### Light（淺色主題）

- 淡灰色背景 + 深灰色文字 + 藍色強調
- 適合：光線充足的環境

### Retro（復古主題）

- 深綠色背景 + 黃綠色文字（仿初代 Game Boy 螢幕效果）
- 適合：喜愛復古風格的玩家

### 主題持久化

- 選擇的主題會存入 NVS（晶片內部快閃記憶體）
- **關機後不會消失**，下次開機會自動套用上次選擇的主題

---

## 12. WiFi 與 OTA 韌體更新

### 事前準備

1. 在 SD 卡建立設定檔 `system/wifi.cfg`：

```
SSID=你的WiFi名稱
PASS=你的WiFi密碼
OTA_URL=http://你的伺服器/firmware.bin
```

2. 確認 OTA_URL 指向一個有效的韌體檔案下載連結

### 更新步驟

1. 進入 **設定畫面**
2. 選擇 **WiFi OTA** 按 **A**
3. 畫面會依序顯示：
   - **「Connecting WiFi...」** — 正在連接 WiFi
   - **「Downloading... X%」** — 正在下載韌體
   - **下載完成後自動重開機**，載入新韌體

### 更新過程中的注意事項

- OTA 在**背景執行**，不會凍結畫面
- 更新過程中，電池讀數會暫停（WiFi 與電池 ADC 共用資源）
- 下載完成後系統會**自動重新啟動**
- 如果新韌體有問題，系統支援 **Rollback**（回滾到上一版）

### 狀態訊息說明

| 訊息 | 原因 |
|------|------|
| **WiFi failed!** | WiFi 連線失敗（名稱或密碼錯誤、超出範圍） |
| **No OTA_URL in wifi.cfg** | 設定檔中缺少 OTA_URL |
| **Downloading... X%** | 正在下載韌體（正常） |
| **OTA failed: ...** | 下載或安裝失敗 |

---

## 13. 電池資訊

如果你的主機板接了 3.7V 鋰電池（經分壓器接到 GPIO15）：

- 在 **設定畫面** 可以看到電池百分比和電壓
- 格式範例：`85% (4050mV)`
- 每 10 秒更新一次讀數
- 在 WiFi OTA 期間，電池讀數會暫時凍結

### 電量對照

| 百分比 | 電壓 | 狀態 |
|--------|------|------|
| 100% | ≥ 4200mV | 充飽 |
| 50% | ~3750mV | 中等 |
| 0% | ≤ 3300mV | 電量耗盡 |

---

## 14. 支援的遊戲格式

### NES（紅白機）

| 項目 | 說明 |
|------|------|
| 副檔名 | `.nes`（iNES 格式） |
| 存放位置 | `roms/nes/` |
| 最大檔案大小 | 2 MB |
| 支援的 Mapper | 0 (NROM)、1 (MMC1)、2 (UxROM)、3 (CNROM)、4 (MMC3) |
| 原始解析度 | 256 × 240 |
| 音訊 | 4 聲道 APU（方波 ×2、三角波、噪音） |

### GameBoy

| 項目 | 說明 |
|------|------|
| 副檔名 | `.gb` |
| 存放位置 | `roms/gb/` |
| 最大檔案大小 | 2 MB |
| 支援的 MBC | MBC1、MBC3、MBC5 |
| 原始解析度 | 160 × 144 |
| 音訊 | 4 聲道 APU（方波 ×2、波形、噪音） |

### 遊戲自動偵測

- 系統根據**副檔名**自動選擇模擬器
- `.nes` → NES 模擬器
- `.gb` → GameBoy 模擬器
- 其他副檔名 → 顯示「Unsupported ROM type」錯誤

### 畫面顯示

兩種遊戲都會自動縮放至 240×320 螢幕顯示，上下會有黑邊（保持正確的畫面比例）。

---

## 15. 常見問題排除

### 開機後看不到任何遊戲

| 可能原因 | 解決方式 |
|---------|---------|
| SD 卡未插入或接觸不良 | 重新插拔 SD 卡 |
| SD 卡不是 FAT32 格式 | 重新格式化為 FAT32 |
| ROM 沒放在正確資料夾 | NES → `roms/nes/`，GB → `roms/gb/` |
| 副檔名不正確 | 確認是 `.nes` 或 `.gb` |

### 遊戲無法啟動

| 錯誤訊息 | 解決方式 |
|---------|---------|
| Invalid iNES magic header | ROM 檔案損壞，請重新取得 |
| Nintendo logo mismatch | GB ROM 檔案損壞或格式不正確 |
| ROM size invalid or too large | ROM 超過 2MB 上限，或檔案為 0 位元組 |
| NES ROM has 0 PRG banks | ROM 標頭不正確 |
| Unsupported ROM type | 副檔名不是 `.nes` 或 `.gb` |

### 遊戲中沒有聲音

1. 按 **VOL+** 確認音量沒有被調到 0%
2. 如果音量已調高仍無聲，重新啟動裝置

### 存檔失敗（Save Failed!）

1. 確認 SD 卡未啟用寫入保護
2. 確認 SD 卡還有剩餘空間
3. 拔出 SD 卡重新插入後再試

### 讀檔顯示「No Save!」

- 你還沒有存過這個遊戲的進度
- 先遊玩、開暫停選單存檔後，才能讀檔

### WiFi 連線失敗

1. 確認 `system/wifi.cfg` 中的 SSID 和 PASS 完全正確（區分大小寫）
2. 確認 WiFi 路由器在有效範圍內
3. 確認網路沒有設定 MAC 位址過濾
4. WiFi 連線逾時為 15 秒，最多嘗試 5 次

### OTA 更新下載途中卡住

1. 確認網路穩定
2. 將裝置移近 WiFi 路由器
3. 重新進入設定畫面再試一次

### 畫面凍結 / 無回應

1. 嘗試按 **B** 返回
2. 如果完全無回應，關閉電源等待 30 秒後重新啟動

---

> **提示：** 遊戲 ROM 需由使用者自行合法取得，本專案僅提供模擬器平台。
